# 06 Export Runtime Pack

Use this folder for exported `.json`, `.skel`, `.atlas`, and packed `.png` files from Spine 4.3. Keep source images in the lesson folders and put runtime exports here so integration tests stay tidy.
